name: 01-building-blocks

on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
          - name: Say hello
            run: echo "Heloo World!"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
          - name: Failed step
            run: |
              echo "I'll Fail"
              exit 1
          - name: Say goodbye
            run: echo "Goodbye!"